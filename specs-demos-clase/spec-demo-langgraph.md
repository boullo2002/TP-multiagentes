# demo02-langgraph spec

Este proyecto fue implementado siguiendo el documento `EJ02-ReAct-LangGraph/spec-back.md`.

**Nota (TP-multiagentes):** el repo del trabajo práctico **no** es este demo minimalista: amplía con MCP, dos grafos LangGraph (Schema + Query), artefacto `schema_context.json`, Open WebUI + Streamlit para Schema Agent, etc. La fuente de verdad del TP es `spec.md` y `src/specs/*.md`.
